# Fase atual — Fase 3

Esta pasta contém somente as SPECs e tasks da Fase 3. A Fase 2 encerrada foi arquivada em `05_entregas/fase-2/` (jornada, matriz, relatórios de fechamento por task e manifest de encerramento com digest).

- `fase.md`: jornada e tabela operacional da Fase 3 (F3-T01..F3-T08).
- `00-Tasks_Gerais.md`: resumo operacional da fase.
- `specs/`: contratos executáveis das entregas — F3-001 (pipeline integrado), F3-002 (dashboard de gargalos), F3-003 (handoffs por papel) e F3-004 (sinais de conta do DHO).

Fase 3 liberada para execução controlada: nenhuma task iniciada. **F3-T01 é a única task elegível** e exige autorização expressa do Champion antes de executar. Bloqueios nomeados: B3-META-01 (regularização da conexão Meta antes de qualquer leitura), B3-ID-01 (identidade por fonte, sem dedupe por inferência), B3-MET-01 (G-001 alvo/fórmula/período antes de KPI de resultado), B3-RACI-01 (matriz nominal de handoffs antes da prova F3-T06), B3-DHO-01 (lista de sinais + política antes de dado real de sinal). Execute apenas uma task por vez e obtenha teste humano antes da seguinte.
